---
- name: Get external IP
  hosts: all
  tasks:
  - name: Get public IP
    community.general.ipify_facts:
    register: IP
  - name: Print public IP
    ansible.builtin.debug:
      msg: "{{ ipify_public_ip }}"
